type Post @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  content: String
}

input AMPLIFY {
  globalAuthRule: AuthRule = {allow: public}
}

type Blog @model {
  id: ID!
  name: String!
}
 